import React from "react";
import { View, Text } from "react-native";
import style from "./style";

const Login = (props) => {
    return (
        <View style={style.container}>
            <Text>emailandpass</Text>
        </View>
    );
};

export default Login;
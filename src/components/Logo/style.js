import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    container: {
        marginTop: "0%",
        // height: "100%"
    },

    image: {
        // height: "100%",
    }
})

export default style